#!/usr/bin/env ruby
# frozen_string_literal: true

lib = File.expand_path('../lib/', __dir__)
$LOAD_PATH.unshift lib unless $LOAD_PATH.include?(lib)

require 'zm-ruby-client'
puts "Zm::Client.version=#{Zm::Client.version}"

require 'get_process_mem'
@mem = GetProcessMem.new
puts @mem.mb

config_path = './test/fixtures/config.yml'
config = Zm::Client::ClusterConfig.new(config_path)
@cluster = Zm::Client::Cluster.new(config)
puts @mem.mb
# @cluster.login
# @domain = @cluster.domains.all.last

require 'irb'
IRB.start(__FILE__)
